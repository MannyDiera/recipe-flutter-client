import 'package:flutter/material.dart';

class SocialScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text(
        "Share recipes/Forum page", 
        style: new TextStyle(fontSize: 20.0),
        ),
    );
  }
}